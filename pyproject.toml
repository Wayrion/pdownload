[project]
name = "pdownload"
version = "0.1.0"
description = "Benchmark plotting tools for Kotlin parallel downloader"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.8",
]
